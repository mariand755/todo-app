# Scoped to the backend:test image scan only (docker_backend.yml) — do not
# reuse this file for the runtime image scan, which should stay unfiltered.
#
# pip-audit (a dev/CI-only dependency, used for our own dependency-audit
# step) transitively depends on pip as a library. pip vendors its own
# bundled copies of msgpack and setuptools under pip/_vendor/ — dead code
# at rest that our test suite and application never import or execute.
# Neither pip nor pip-audit are present in the runtime image (see the
# runtime stage in backend/Dockerfile), so this exposure is scoped entirely
# to CI tooling. No pip release has patched its vendored msgpack as of this
# writing, so there is no version bump that resolves this.
CVE-2025-47273
GHSA-6v7p-g79w-8964
