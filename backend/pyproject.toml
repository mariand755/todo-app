[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorist==1.8.13",
    "fastapi>=0.118.0",

    # Explicit overrides to avoid known vulnerable transitive versions
    "starlette>=1.0.0",
    "python-multipart>=0.0.27",
    "urllib3>=2.6.3",
    "psycopg2-binary>=2.9.11",
    "sqlalchemy>=2.0.49",
    "uvicorn>=0.44.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.11",
    "pytest-cov>=7.1.0",
    "mypy>=1.20.1",
    "httpx>=0.28.1",
    "pre-commit>=4.5.1",
    "pip-audit>=2.10.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --maxfail=1 -m 'not integration' -W error"
markers = [
  "unit: fast tests that do not require external services",
  "integration: tests that require external services like Postgres",
  "contract: API contract and request/response behavior tests",
  "persistence: data persistence, ordering, and consistency tests",
]

[tool.ruff]
line-length = 120
target-version = "py312"
[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["B008"]

[tool.mypy]
python_version = "3.12"
strict = false
ignore_missing_imports = true
plugins = ["sqlalchemy.ext.mypy.plugin"]
